{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "array",
	"minItems": 1,
	"items": {
		"type": "object",
		"properties": {
			"choiceField": {
				"type": "string",
				"description": "The name of the choice field in the list."
			},
			"choiceValue": {
				"type": "string",
				"description": "The value of the choice field that triggers the update."
			},
			"lookupField": {
				"type": "string",
				"description": "The name of the lookup field to update."
			},
			"lookupValue": {
				"type": "string",
				"description": "The value to set in the lookup field."
			}
		},
		"anyOf": [
			{
				"required": ["choiceField", "lookupField"]
			},
			{
				"required": ["choiceField", "lookupField", "choiceValue", "lookupValue"]
			}
		]
	}
}
